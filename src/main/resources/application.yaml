spring:
  application:
    name: watchmyai-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/watchmyai
    username: watchmyai
    password: watchmyai

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true